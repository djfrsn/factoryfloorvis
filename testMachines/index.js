// Groupings of Test Queues(stored as hash) will map to Machine ID
import machinesList from './machineList';

export function queueOrders(orders, partsList) {
  console.log(orders);
  console.log(partsList);
}
